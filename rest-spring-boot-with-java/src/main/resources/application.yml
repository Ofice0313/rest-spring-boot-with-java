cors:
  originPatterns: http://localhost:8080, http://localhost:3000
spring:
  application:
    name: rest-spring-boot-with-java
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_spring_boot?useSSL=false&useTimezone=true&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: springstudent
    password: "springstudent"

  jpa:
    hibernate:
      ddl-auto: update  # update => ou 'create', 'validate', 'none'
    show-sql: true      # Para ver SQL no console
    properties:
      hibernate:
        # dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true  # Formata o SQL para melhor leitura
    open-in-view: false

  jackson:
    serialization:
      indent-output: true
  # Configura��o do Flyway
#  flyway:
#    enabled: true
#    locations: classpath:db/migration
#    baseline-on-migrate: true
#    baseline-version: 1
#    validate-on-migrate: true
#    # Se quiser limpar e recriar (APENAS DESENVOLVIMENTO):
#    # clean-disabled: false
#    schemas: rest_spring_boot

# Configura��es adicionais �teis
logging:
  level:
    root: INFO
    # org.springframework: INFO
    # org.hibernate.SQL: DEBUG      # Mostra SQL
    # org.hibernate.type: TRACE     # Mostra valores dos par�metros
    # com.devcaleb: INFO
spring-doc:
  paths-to-match: /api/**/v1/**
  swagger-ui:
    use-root-path: true

testcontainers:
  docker:
    socket:
      override: npipe:////./pipe/docker_engine